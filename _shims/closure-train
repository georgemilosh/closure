#!/bin/bash
# Shim entry-point for closure-train.
# Works without `pip install` – relies on PYTHONPATH set by activate_hpc.sh.
# Uses -m so that Lightning's DDP subprocess re-launch works correctly.
exec python -m closure.cli "$@"
